project_id = "dada-listening"

[db]
port = 54322
major_version = 15

[storage]
enabled = true
file_size_limit = "500MiB"

[auth]
enabled = true
# 학생은 가입하지 않는다 (D-005). auth.users 의 모든 행은 강사다.
# 강사 초대 흐름을 만들기 전까지 공개 가입은 닫아 둔다.
enable_signup = false

[auth.email]
enable_signup = false
enable_confirmations = false
