# Supabase 프로젝트 설정 → API 에서 복사.
# anon key 는 공개되어도 되는 키다. 실제 권한은 RLS 가 담당한다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 아래는 절대 여기에 넣지 않는다 (Edge Function 시크릿으로만 설정):
#   SUPABASE_SERVICE_ROLE_KEY
#   ASR_API_KEY
